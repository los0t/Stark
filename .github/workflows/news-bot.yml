name: KAT-TON AI

on:
  workflow_dispatch:

jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - name: Info
        run: echo "KAT-TON AI uses Claude API directly from the browser. No server-side processing needed."
